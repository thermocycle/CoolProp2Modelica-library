within CoolProp2Modelica.Common;
type InputChoiceCoolProp = enumeration(
    dT "(d,T) as inputs",
    ph "(p,h) as inputs",
    ps "(p,s) as inputs",
    pT "(p,T) as inputs",
    hs "(h,s) as inputs");
